{
  "name": "ster-js",
  "version": "0.0.0",
  "private": true,
  "description": "JavaScript linting for ster's browser-side graph assets (not published).",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "eslint .",
    "check": "node --check ster/assets/graph_app.js && node --check kai-extension/popup.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0"
  }
}
